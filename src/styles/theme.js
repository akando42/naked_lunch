// Here a theme is defined. This theme is passed to the entire site (via layout.js) so the individual parts of the theme object 
// can be accessed in each styled component.

export const theme = {
    colors: {
        main: 'yellow;', //used in header
        primaryAccent: 'yellow', //used for thumbnails on homepage and as price background
        secondaryAccent: 'black', //used for header border and item buttons
    }
};